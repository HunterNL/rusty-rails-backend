{
	default_bind localhost
	skip_install_trust
	local_certs
	admin off
	http_port 8080
	https_port 4343
}

api.dev.localhost {
	reverse_proxy localhost:9001
	encode zstd gzip
	log {
		format console
	}
}
